per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}
deposit = []
money = 100000
for percent in per_cent.values():
    deposit.append(money * percent / 100)
max_deposit = max(deposit);
print("максимальное значение: " + str(max_deposit))